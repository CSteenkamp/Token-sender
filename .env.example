# Copy this file to .env.local and configure your settings
# Most values can be left as defaults for preprod testnet

# Cardano Network (preprod, preview, mainnet)
NEXT_PUBLIC_CARDANO_NETWORK=preprod

# Blockfrost API Keys for Token Metadata (Optional but recommended)
# Get your free API keys from: https://blockfrost.io
NEXT_PUBLIC_BLOCKFROST_MAINNET_PROJECT_ID=your_mainnet_project_id
NEXT_PUBLIC_BLOCKFROST_PREPROD_PROJECT_ID=your_preprod_project_id
NEXT_PUBLIC_BLOCKFROST_PREVIEW_PROJECT_ID=your_preview_project_id

# Note: Without Blockfrost API keys, token metadata (names/images) won't load
# But the app will still function for sending transactions